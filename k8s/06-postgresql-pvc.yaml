apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blog-cms-postgresql-pvc
  namespace: blog-cms
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
